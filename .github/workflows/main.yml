name: run-simple-use-case

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  run-aiida:
    runs-on: ubuntu-latest
    # docker pull aiidateam/aiida-prerequisites:0.4.0
    container: 
      image: aiidateam:aiida-prerequisites-0.4.0

    steps:
      - name: hello
        shell: bash -l {0}
        run: echo "Hello"
